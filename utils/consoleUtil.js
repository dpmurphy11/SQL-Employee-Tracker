// module for faormatting and outputing recordeset data to console
const cTable = require('console.table');

const showSplash = () => {
    // show splash screen
}

const renderOutput = (rs) => {
    // render the recordset to console using CTable
}
module.exports = consoleUtil;